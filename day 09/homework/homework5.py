# Creating a list of numbers
numbers = [10, 20, 30, 40, 50]

# Loop through each number in the list
for num in numbers:  # 'num' is the iteration variable
    # The iteration variable 'num' takes the value of each element in the list one by one
    print(num)  # Print the current value of 'num'