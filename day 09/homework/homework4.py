# Creating a list of numbers
numbers = [1, 2, 3, 4, 5]

# Loop through each number in the list
for num in numbers:
    # This block of code will run for each element in the list
    print(num)  # Prints the current number